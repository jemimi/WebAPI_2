using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace WebAPI_2.Controllers
{
    public class HostingCostController : ApiController
    {
        /// <summary>
        /// <example>api/HostingCost {id}</example>
        /// </summary>
        /// Identify Variables - need 3 outputs
        ///Fortnight: How many fortnights and the cost of the fortnight got the first line
        ///Tax: Then 0.13 x would be the tax and round to 2 decimals
        /// Total: Fornight and tax total
        /// <returns></returns>
        
       // public IEnumerable<string> Get(int id)
        //{
           // decimal tax = 0.13;
            //decimal cost = (id / 14) + 1 * 5.50;
           
            //decimal totalTax = tax * cost;
            //string a = "fortnights at $5.50/FN = $" + cost + " CAD" ;
            //string b = "HST 13% = $" + totalTax + "CAD";
            //string c = "Total = $" + (cost * tax);

            //return new string[] { a, b, c };
           
       // }

        

    }
}
